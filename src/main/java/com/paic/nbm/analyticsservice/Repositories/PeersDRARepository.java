package com.paic.nbm.analyticsservice.Repositories;

import com.paic.nbm.analyticsservice.Entities.PeersDRA;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PeersDRARepository extends JpaRepository<PeersDRA, Integer> {
}
